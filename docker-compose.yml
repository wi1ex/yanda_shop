version: "3.8"
services:

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
  backend:
    build: ./backend
    ports:
      - "9000:8000"

  bot:
    build: ./bot
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_ID: ${ADMIN_ID}
    depends_on:
      - backend
    x-tw-secrets:
      - BOT_TOKEN
      - ADMIN_ID